#include <iostream>
#include <cmath>
#include <cstring>
#include <algorithm>
#include <functional>
using namespace std;

int main()
{
	int n;
	scanf("%d", &n);

	int max = 1;
	for (int i = 0; i < n; i++)
	{
		int inp;
		scanf("%d", &inp);

		max += inp - 1;
	}

	printf("%d", max);
}
